/**
 * Text processing utilities for PDF parsing
 */
export interface TextPosition {
    text: string;
    x: number;
    y: number;
    width: number;
    fontSize: number;
    charSpacing: number;
    wordSpacing: number;
}
/**
 * Process text positions into a final string with proper spacing and line breaks
 */
export declare function processTextFromPositions(positions: TextPosition[]): string;
