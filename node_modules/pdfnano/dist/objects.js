"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.PDFStream = exports.PDFDictionary = exports.PDFArray = exports.PDFName = exports.PDFString = exports.PDFNumber = exports.PDFBoolean = exports.PDFNull = exports.PDFObject = exports.PDFReference = void 0;
exports.createPDFObject = createPDFObject;
/**
 * Classes representing PDF objects
 */
const decoders_1 = require("./decoders");
/**
 * PDF object reference
 */
class PDFReference {
    constructor(objectNumber, generation = 0) {
        this.objectNumber = objectNumber;
        this.generation = generation;
    }
    toString() {
        return `${this.objectNumber} ${this.generation} R`;
    }
}
exports.PDFReference = PDFReference;
/**
 * Base PDF object
 */
class PDFObject {
    constructor(objectNumber, generation = 0) {
        this.objectNumber = objectNumber;
        this.generation = generation;
    }
    getReference() {
        if (this.objectNumber === undefined)
            return undefined;
        return new PDFReference(this.objectNumber, this.generation || 0);
    }
}
exports.PDFObject = PDFObject;
/**
 * PDF Null object
 */
class PDFNull extends PDFObject {
    toString() {
        return 'null';
    }
}
exports.PDFNull = PDFNull;
/**
 * PDF Boolean object
 */
class PDFBoolean extends PDFObject {
    constructor(value, objectNumber, generation) {
        super(objectNumber, generation);
        this.value = value;
    }
    toString() {
        return this.value ? 'true' : 'false';
    }
}
exports.PDFBoolean = PDFBoolean;
/**
 * PDF Number object
 */
class PDFNumber extends PDFObject {
    constructor(value, objectNumber, generation) {
        super(objectNumber, generation);
        this.value = value;
    }
    toString() {
        return this.value.toString();
    }
}
exports.PDFNumber = PDFNumber;
/**
 * PDF String object
 */
class PDFString extends PDFObject {
    constructor(value, isHex = false, objectNumber, generation) {
        super(objectNumber, generation);
        this.value = value;
        this.isHex = isHex;
    }
    toString() {
        if (this.isHex) {
            return `<${this.value}>`;
        }
        return `(${this.value.replace(/\\/g, '\\\\').replace(/\(/g, '\\(').replace(/\)/g, '\\)')})`;
    }
}
exports.PDFString = PDFString;
/**
 * PDF Name object
 */
class PDFName extends PDFObject {
    constructor(name, objectNumber, generation) {
        super(objectNumber, generation);
        this.name = name.startsWith('/') ? name : `/${name}`;
    }
    toString() {
        return this.name;
    }
}
exports.PDFName = PDFName;
/**
 * PDF Array object
 */
class PDFArray extends PDFObject {
    constructor(items = [], objectNumber, generation) {
        super(objectNumber, generation);
        this.items = items;
    }
    get(index) {
        return this.items[index];
    }
    add(item) {
        this.items.push(item);
    }
    get length() {
        return this.items.length;
    }
    toString() {
        return `[${this.items.map(item => item.toString()).join(' ')}]`;
    }
}
exports.PDFArray = PDFArray;
/**
 * PDF Dictionary object
 */
class PDFDictionary extends PDFObject {
    constructor(entries = {}, objectNumber, generation) {
        super(objectNumber, generation);
        this.entries = new Map();
        if (entries instanceof Map) {
            entries.forEach((value, key) => {
                this.set(key, value);
            });
        }
        else {
            Object.entries(entries).forEach(([key, value]) => {
                this.set(key, value);
            });
        }
    }
    get(key) {
        const normalizedKey = key.startsWith('/') ? key : `/${key}`;
        return this.entries.get(normalizedKey);
    }
    set(key, value) {
        const normalizedKey = key.startsWith('/') ? key : `/${key}`;
        this.entries.set(normalizedKey, value);
    }
    has(key) {
        const normalizedKey = key.startsWith('/') ? key : `/${key}`;
        return this.entries.has(normalizedKey);
    }
    getType() {
        const type = this.get('Type');
        return type ? type.name : undefined;
    }
    toString() {
        const entries = Array.from(this.entries.entries())
            .map(([key, value]) => `${key} ${value.toString()}`)
            .join(' ');
        return `<<${entries}>>`;
    }
}
exports.PDFDictionary = PDFDictionary;
/**
 * PDF Stream object
 */
class PDFStream extends PDFObject {
    constructor(dictionary, data, objectNumber, generation) {
        super(objectNumber, generation);
        this.dictionary = dictionary;
        this.data = data;
    }
    /**
     * Get decoded stream data
     */
    getDecodedData() {
        if (this.decodedData) {
            return this.decodedData;
        }
        // Get filters
        let filters = [];
        const filter = this.dictionary.get('Filter');
        if (filter instanceof PDFName) {
            filters = [filter.name];
        }
        else if (filter instanceof PDFArray) {
            filters = filter.items.map(item => item.name);
        }
        // Get decode parameters
        let decodeParms = null;
        const params = this.dictionary.get('DecodeParms');
        if (params) {
            decodeParms = params;
        }
        // Decode the data
        this.decodedData = filters.length > 0
            ? decoders_1.StreamDecoder.decode(this.data, filters, decodeParms)
            : this.data;
        return this.decodedData;
    }
}
exports.PDFStream = PDFStream;
/**
 * Creates PDF objects from primitive JavaScript values
 * @param value The value to convert to a PDF object
 * @returns PDF object
 */
function createPDFObject(value) {
    if (value === null || value === undefined) {
        return new PDFNull();
    }
    if (typeof value === 'boolean') {
        return new PDFBoolean(value);
    }
    if (typeof value === 'number') {
        return new PDFNumber(value);
    }
    if (typeof value === 'string') {
        if (value.startsWith('/')) {
            return new PDFName(value);
        }
        return new PDFString(value);
    }
    if (Array.isArray(value)) {
        return new PDFArray(value.map(item => createPDFObject(item)));
    }
    if (value instanceof Buffer) {
        const dict = new PDFDictionary({
            Length: new PDFNumber(value.length)
        });
        return new PDFStream(dict, value);
    }
    if (typeof value === 'object') {
        const entries = new Map();
        Object.entries(value).forEach(([key, val]) => {
            entries.set(key.startsWith('/') ? key : `/${key}`, createPDFObject(val));
        });
        return new PDFDictionary(entries);
    }
    return new PDFNull();
}
