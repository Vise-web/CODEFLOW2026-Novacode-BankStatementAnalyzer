import { PDFObject, PDFDictionary } from './objects';
export declare const DEBUG: boolean;
/**
 * Represents a PDF cross-reference entry
 */
export interface XRefEntry {
    offset: number;
    generation: number;
    inUse: boolean;
    compressed?: boolean;
    objStreamNum?: number;
}
/**
 * Represents a PDF document structure
 */
export declare class PDFStructure {
    /** PDF Version */
    version: string;
    /** Cross-reference table entries */
    xref: Map<number, XRefEntry>;
    /** PDF trailer dictionary */
    trailer: PDFDictionary;
    /** Object cache */
    objectCache: Map<number, PDFObject>;
    /** Raw buffer containing the PDF file */
    buffer: Buffer;
    /** Root catalog object */
    rootCatalog?: PDFDictionary;
    /** Info dictionary */
    info?: PDFDictionary;
    /** Object retrieval count */
    private objectRetrievalCount;
    /** Object parse guard to prevent recursion */
    private currentlyParsingObjects;
    constructor(buffer: Buffer);
    /**
     * Parse the PDF document structure
     */
    parse(): void;
    /**
     * Extract PDF version from the header
     */
    private extractPDFVersion;
    /**
     * Find the startxref position in the PDF
     */
    private findStartXRef;
    /**
     * Read the xref table offset from the startxref position
     */
    private readStartXRef;
    /**
     * Parse xref table chain, following Prev references for incremental updates
     */
    private parseXRefChain;
    /**
     * Parse the cross-reference table
     * @returns Previous xref offset if Prev exists, null otherwise
     */
    private parseXRef;
    /**
     * Parse trailer dictionary at a specific position and extract Prev offset
     */
    private parseTrailerForPrev;
    /**
     * Parse a cross-reference stream (used in PDF 1.5+)
     */
    private parseXRefStream;
    /**
     * Parse the data from an xref stream
     */
    private parseXRefStreamData;
    /**
     * Read an integer from a buffer at a given position with specified length
     */
    private readIntFromBuffer;
    /**
     * Reconstruct the xref table by scanning for objects in the PDF
     */
    private reconstructXRefFromObjects;
    /**
     * Scan a chunk of the buffer for PDF objects
     * Returns the number of objects added
     */
    /**
     * Check if a byte code is a whitespace character
     */
    private isWhitespace;
    /**
     * Scan a chunk of the buffer for PDF objects
     * Returns the number of objects added
     */
    private scanBufferChunkForObjects;
    /**
     * Scan found objects to identify and process Object Streams
     */
    private scanForObjectStreams;
    /**
     * Process an Object Stream and add contained objects to xref
     */
    private processObjectStream;
    /**
     * Read integer from buffer, skipping whitespace
     */
    private readIntFromData;
    /**
     * Parse a compressed object from an Object Stream
     */
    private parseCompressedObject;
    /**
     * Find the trailer dictionary in a reconstructed xref table
     */
    private findTrailerInReconstructedXRef;
    /**
     * Find the root catalog in the PDF objects
     */
    private findRootCatalog;
    /**
     * Parse the trailer dictionary
     */
    private parseTrailer;
    /**
     * Parse a PDF object at the given offset
     */
    private parseObject;
    /**
     * Get an object by number and generation
     */
    getObject(objectNumber: number, generation?: number): PDFObject;
    /**
     * Parse a PDF value at the given position
     */
    private parseValue;
    /**
     * Parse a PDF string
     */
    private parseString;
    /**
     * Parse a PDF hexadecimal string
     */
    private parseHexString;
    /**
     * Parse a PDF name
     */
    private parseName;
    /**
     * Parse a PDF array
     */
    private parseArray;
    /**
     * Parse a PDF dictionary
     */
    private parseDictionary;
    /**
     * Parse a PDF stream
     */
    private parseStream;
    /**
     * Parse a PDF number
     */
    private parseNumber;
    /**
     * Parse a number or reference
     */
    private parseNumberOrReference;
}
