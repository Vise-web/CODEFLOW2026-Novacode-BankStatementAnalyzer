import { PDFStructure } from './structure';
import { PDFPage, ParserOptions } from './types';
/**
 * Extracts pages from the PDF structure
 * @param structure PDF structure info
 * @param options Parser options (for maxPages)
 * @returns Array of PDF pages
 */
export declare function extractPages(structure: PDFStructure, options: ParserOptions): PDFPage[];
